# 코드 구조 지도

이 저장소는 **하나의 공용 라이브러리(`core/`) + 실험별 폴더**로 되어 있다.
"어떤 실험이 어디 있나"와 "어떤 코드가 공용인가"만 알면 나머지는 각 파일의
맨 위 docstring 이 설명한다.

---

## 원칙 두 가지

1. **실험 폴더는 서로를 import 하지 않는다.**
   두 실험 이상이 같은 코드를 쓰게 되면 그건 실험이 아니라 인프라이므로 `core/`
   로 옮긴다. (예전엔 `report/exp_*.py` 가 `fusion/ablation.py` 를, 실험 4개가
   `late_fusion_tab_image.py` 를 서로 import 하고 있었다.)
2. **같은 개념은 한 곳에만 정의한다.**
   모델 구조·결합 절차·평가 지표·그림 팔레트가 여러 파일에 복사돼 있으면,
   한쪽만 고쳤을 때 두 표가 서로 다른 정의로 계산돼도 아무도 모른다.

---

## 폴더 지도

| 폴더 | 무슨 실험인가 | 결론 (RESULTS.md 절) |
|---|---|---|
| `core/` | (실험 아님) 모든 실험이 쓰는 공용 라이브러리 | — |
| `실험1_기본융합_early_late/` | early(concat) vs late(가중합) 융합 기본 비교 + **최종 채택 모델** | §8 · OS 0.7143 |
| `실험2_학습조건_배치크기진단/` | batch=16 이 Cox risk set 을 좁혀 tabular 를 과소학습시키나? | §4 · **그렇다** |
| `실험3_모달리티_절제실험/` | 재료(모달리티)를 하나씩 빼면? | §3·§5·§9.3 |
| `실험4_영상_발언권독점_진단/` | concat 에서 영상이 발언권 70~80% 독점 — 원인인가? | §9.4 · **증상일 뿐** |
| `실험5_late융합_영상기여도_검정/` | late fusion 에서 OS만 오르고 PFS는 왜 떨어지나? | §10 · **영상에 PFS 정보 없음** |
| `실험6_판독지_인코더_비교/` | 판독지 인코더 TF-IDF vs RadBERT, 텍스트 어느 부분, SUV 수치 | 단독은 RadBERT 승, 결합하면 무승부 |
| `실험7_임상변수_결측처리/` | 전체-중앙값 대치(누수)를 fold-safe 로 고치면 좋아지나? | **성능은 그대로**, 방법론만 정확해짐 |
| `실험8_영상단독_고전CoxPH/` | 영상 백본은 두고 위험예측만 고전 CoxPH 로 바꾸면? | 전부 DeepSurv 이하 |
| `실험9_병기별_stage_aware_융합/` | LS/ES 가 서로 다른 융합 가중치를 배워야 하나? | 근거 없음 |
| `도구/` | 그림 생성, 체크포인트 덤프 | — |

각 실험 폴더의 `RESULTS_*.md` / `README.md` 가 그 실험의 상세 기록이다.

---

## `core/` — 공용 라이브러리

| 모듈 | 책임 |
|---|---|
| `cohort.py` | 238명 코호트 로딩 · 매니페스트 · `brain_meta` 누수 수정 |
| `dataset.py` | PNG 로딩 · `(image, tabular)` Dataset · fold별 test set 재구성 |
| `features.py` | 임상 인코더 · TF-IDF · 판독지 코퍼스 · fold별 결합 텐서 |
| `model.py` | 이미지 백본 · 브랜치 · **`ConcatDeepSurv`** · `MODALITY_CONFIGS` |
| `train.py` | Cox 손실 · 학습 루프 · `TrimodalEvaluator`/`ImageOnlyEvaluator` · `fold_plan` |
| `fusion_stack.py` | 단일모달 OOF 위험점수 추출 + **CoxPH 가중합 결합** |
| `metrics.py` | C-index 부호 관례 · 과적합 격차 · fold 쌍대 검정 |
| `plotstyle.py` | 그림 팔레트 · rcParams · 한글 폰트 fallback |
| `reporting.py` | 프로토콜 11절 산출물 폴더 작성 |

자기점검이 있는 모듈은 패키지로 실행한다: `python -m core.cohort`

---

## 실험 스크립트가 core 를 쓰는 법

폴더가 한 단계 아래이므로 파일 맨 위에 프로젝트 루트를 `sys.path` 에 넣는
3줄짜리 앵커가 있고, 그 뒤로는 평범한 import 다.

```python
import os
import sys

PROJECT_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, PROJECT_ROOT)

from core import cohort, features
from core.model import MODALITY_CONFIGS, make_model_factory
from core.train import TrimodalEvaluator
```

같은 폴더 안의 형제 모듈(`import ko2en` 등)은 파이썬이 스크립트 폴더를
`sys.path[0]` 에 자동으로 넣으므로 그냥 import 하면 된다.

---

## 이번 정리에서 합치거나 지운 것

### 합친 것 (중복 제거)

| 무엇 | 전 | 후 |
|---|---|---|
| concat 융합 모델 | `TrimodalConcatDeepSurv` + `AblatableConcatDeepSurv` + `FixedConcatDeepSurv` — 브랜치 정의가 글자 단위로 같고 정규화 처리만 달랐다 | `core.model.ConcatDeepSurv` (`use_*` 플래그 + `norm_mode`). **속성 이름을 유지해 기존 체크포인트 272개가 strict 로드된다** |
| 모달 조합표 | `ablation.CONFIGS` (실험 파일에 있는데 10여 개가 import) | `core.model.MODALITY_CONFIGS` |
| 모델 공장 | `make_factory` 3벌 (ablation / fusion_fix / balance_dims) | `core.model.make_model_factory` |
| CoxPH 결합 | `combine_two`(2변수) + `combine_weighted_sum`(3변수) — 절차가 동일 | `core.fusion_stack.combine_risk_scores` + 이름 유지용 얇은 래퍼 2개 |
| OOF 헬퍼 | `_oof_dict` / `_oof_lookup` / `_labels_by_id` 각 2벌 | `core.fusion_stack.oof_dict` / `labels_by_id` |
| OOF 추출기 | `late_fusion_tab_image` 안에 있는데 실험 4개가 import | `core.fusion_stack.get_tabular_oof` 등 |
| 과적합 격차 계산 | 4개 파일에 같은 4줄 루프 | `core.metrics.train_val_gap` |
| 쌍대 검정 | `exp_bert_text` 안에만 | `core.metrics.paired_pvalues` |
| 그림 팔레트/폰트 | 4개 파일에 복붙 | `core.plotstyle` |
| fold test set 재구성 | `analyze_contribution` + `analyze_redundancy` 에 각각 20여 줄 | `core.dataset.build_fold_test_dataset` |
| 단일모달 pycox arm | `run_clinical_only` / `run_report_only` 가 인코더만 빼고 동일 | `late_fusion_3modal._run_pycox_arm` |
| 실험 계약(contract) | `main.py` 의 early/late 블록에 30줄 넘게 동일 문자열 | `main._base_contract` |
| `_fold_plan` / `_seed_everything` | 밑줄 private 인데 12곳에서 import | 공개 이름 `fold_plan` / `seed_everything` |

### 지운 것

| 파일/심볼 | 이유 |
|---|---|
| `generate_report.py` | **RESULTS.md 를 통째로 덮어쓴다.** 이 문서는 그 뒤 1000줄 넘게 손으로 쓴 것이라 실행하면 날아가는 함정이었다. 아직 쓰이는 fig1 만 `도구/plot_all_figures.py` 로 옮기고 삭제 |
| fig2(fold 산포) · fig3(late 가중치) | 어느 문서도 참조하지 않음 (fig2 는 PNG 파일조차 없었다) |
| `model.parameter_counts()` | 정의만 있고 호출부 0 |
| `exp_radbert_full.py` 의 모듈 레벨 실행 | import 만 해도 학습이 시작됐다 → `main()` 으로 감쌈 |
| 미사용 import (`glob`, `sys`, `torch`, `get_image_oof_resnet18` 등) | — |

`exp_ogm_ge.py`(§9.7.3 OGM-GE)와 `exp_radbert_concat.py` 는 이번 정리 **이전에**
이미 없어진 파일이다. 결과는 `outputs/ogm_ge/`, `outputs/radbert_concat/` 와
각 `RESULTS_*.md` 에 남아 있으며, 문서의 해당 명령에 그 사실을 적어 두었다.

---

## 정리가 수치를 바꾸지 않았다는 확인

| 확인 | 결과 |
|---|---|
| 옛 모델 3종 vs `ConcatDeepSurv`: state_dict 키 · 파라미터 · forward 출력 | 6개 조합 전부 **비트 단위 동일** |
| 기존 체크포인트 strict 로드 | **272 / 272 성공** |
| `combine_two` 로 `outputs/late_fusion_B/results.json` 재현 | OS 0.7221 / PFS 0.6531 — **Δ = 0.000000** |
| `python main.py --experiment early_fusion --mode smoke_test` | PASS |
| 전 모듈 import + 실학습 2 epoch 파이프라인 | 통과 |
