# 실험 리포트: "발언권 문제"를 구조 수정으로 고칠 수 있을까?

> [RESULTS.md](RESULTS.md) **9.7절의 3번·4번 제안**을 실제로 돌려본 결과.
> 같은 환자 238명, 같은 5-fold, 같은 학습조건(batch 32 / 60 epoch).
> 지표는 C-index (0.5 = 동전 던지기, 1.0 = 완벽).

---

## 0. 세 줄 요약

1. **두 제안 다 실패했다.** 어떤 조합도 기존 최고 모델(Clinical+Report)을 넘지 못했다.
2. **그런데 실패한 이유를 알아낸 게 진짜 수확이다.** 3번 제안(학습 가능한 손잡이)은
   **수학적으로 애초에 아무 효과가 없는 방법**이었다. 실험이 이를 증명했다.
3. 덕분에 **이전 진단(RESULTS.md 9.4)의 오류가 드러났고**, 다음에 무엇을 해야 하는지가
   훨씬 분명해졌다 → 5장·6장.
4. ⚠️ **단, PFS는 이 방향(fusion 개선) 전체로 해결되지 않는다.** 후속 분석
   ([RESULTS.md](RESULTS.md) 섹션 10)에서 **PFS는 이미지에 고유 정보가 아예 없음**이
   확인되었다(이미지를 난수로 바꿔도 성능 동일). → **6.5장** 참조.

---

## 1. 무엇을 시험했나

이전 실험([RESULTS.md](RESULTS.md) 9장)에서 알아낸 문제는 이랬다.

> 세 모달리티(영상·임상·판독지)를 이어붙여(concat) 학습시키면, **영상이 최종 판단의
> 70~80%를 혼자 차지**해 버린다. 그런데 영상의 단독 실력은 특별히 좋지 않아서,
> 잘하던 임상+판독지 팀이 밀려나고 전체 성능이 떨어진다.

그래서 9.7절에서 두 가지 해법을 제안했었다.

### 3번 제안 — "영상의 목소리 크기를 줄일 수 있게 하자"

기존 코드는 각 브랜치의 출력을 `F.normalize`로 **크기를 무조건 1로 맞춰서** 이어붙인다.
"그래서 영상이 못 미더워도 항상 같은 크기로 말하는 것 아니냐"는 것이 당시 진단이었다.
두 가지 방법을 시험했다.

| 방법 | 무엇을 바꾸나 |
|---|---|
| **nonorm** | 크기 맞추기(`F.normalize`)를 **아예 없앰**. 브랜치가 스스로 목소리 크기를 정하게 함 |
| **scale** | 크기를 1로 맞춘 뒤, 브랜치마다 **"음량 손잡이" 하나씩**을 달아줌. 이 손잡이도 학습으로 조절됨 |

### 4번 제안 — "판독지에게 더 넓은 자리를 주자"

기존 구조에서 각 브랜치가 만드는 특징의 크기는 **영상 128칸 / 임상 128칸 / 판독지 16칸**이다.
가장 유용한 판독지가 가장 좁은 자리만 받고 있었다. 그래서 판독지 자리를 넓혀봤다.

| 방법 | 판독지 칸 수 |
|---|---|
| (기존) | 16칸 |
| **rep64** | 64칸 |
| **rep128** | 128칸 |

그리고 3번과 4번을 **같이** 적용한 조합(`scale_rep64`, `scale_rep128`)도 시험했다.

---

## 2. 실험 설계 — 공정하게 비교하기 위해

**바꾼 것은 오직 위 두 가지뿐이다.** 환자·분할·학습조건·랜덤시드는 전부 고정했다.

**대조군을 두 종류 넣었다.**

| 대조군 | 왜 필요한가 |
|---|---|
| `base` | 아무것도 안 고친 기존 3-modal. **기존 결과와 똑같이 나와야** 코드가 맞다는 뜻 |
| `cr_base` | 기존 최고 모델(Clinical+Report, 영상 없음). **넘어야 할 목표선** |
| `cr_rep64`, `cr_rep128` | 영상 **없이** 판독지 자리만 넓힌 것. 판독지 확대가 "fusion을 고친 것"인지 아니면 "그냥 판독지 브랜치가 좋아진 것"인지 **구분**하기 위함 |

> ✅ **신뢰성 확인:** `base`는 OS **0.6775** / PFS **0.6390**, `cr_base`는 OS **0.7076** /
> PFS **0.6678** 로 기존 RESULTS.md 수치와 **소수점 4자리까지 정확히 일치**했다.
> 실험 코드가 올바르다는 뜻이다.

---

## 3. 결과

**표 1. 전체 결과 (5-fold 평균 ± 표준편차)** ← *PPT 핵심 표*

| 변형 | 영상 사용 | 고친 내용 | OS | PFS |
|---|---|---|---|---|
| `base` | ○ | (없음, 기존) | 0.6775 ± 0.037 | 0.6390 ± 0.047 |
| `nonorm` | ○ | 3번: 크기맞추기 제거 | 0.6789 ± 0.055 | 0.6306 ± 0.052 |
| `scale` | ○ | 3번: 음량 손잡이 | 0.6976 ± 0.062 | 0.6450 ± 0.051 |
| `rep64` | ○ | 4번: 판독지 64칸 | 0.6744 ± 0.072 | 0.6353 ± 0.045 |
| `rep128` | ○ | 4번: 판독지 128칸 | 0.6904 ± 0.058 | 0.6375 ± 0.059 |
| `scale_rep64` | ○ | 3번+4번 | 0.6894 ± 0.054 | **0.6507** ± 0.059 |
| `scale_rep128` | ○ | 3번+4번 | 0.6870 ± 0.054 | 0.6256 ± 0.048 |
| **`cr_base`** | ✕ | **기존 최고 모델** | **0.7076** ± 0.047 | **0.6678** ± 0.034 |
| `cr_rep64` | ✕ | 4번만 (영상 없이) | 0.6832 ± 0.067 | 0.6286 ± 0.069 |
| `cr_rep128` | ✕ | 4번만 (영상 없이) | 0.6972 ± 0.051 | 0.6527 ± 0.022 |

**표 2. 목표선을 넘었는가?**

| 타깃 | 가장 좋았던 3-modal | 점수 | 기존 최고(cr_base) | 차이 | 통계적으로 의미있나 |
|---|---|---|---|---|---|
| OS | `scale` | 0.6976 | 0.7076 | **−0.010** | ✕ (p=0.66) |
| PFS | `scale_rep64` | 0.6507 | 0.6678 | **−0.017** | ✕ (p=0.47) |

**→ 목표선(기존 최고 모델)을 넘은 변형은 하나도 없다.**

참고로 아무것도 안 고친 `base`와 비교하면 조금 올라가긴 했다(OS +0.020, PFS +0.012).
하지만 이 역시 **통계적으로 의미 없는 수준**이고(p=0.22 / 0.54), fold별 편차(±0.05~0.06)보다
작다. **우연일 가능성이 높다.**

---

## 4. 왜 실패했나

### 4.1 3번 제안은 "수학적으로 아무 효과가 없는" 방법이었다 ★핵심

실험 중 이상한 점을 발견했다. **모델이 학습한 "음량 손잡이" 값이 1.0에서 거의 안 움직였다.**

| 변형 | 영상 손잡이 | 임상 손잡이 | 판독지 손잡이 |
|---|---|---|---|
| `scale` (PFS) | 1.025 | 1.004 | 1.022 |
| `scale_rep64` (PFS) | 1.022 | 1.004 | 1.026 |
| `scale_rep128` (PFS) | 1.018 | 1.002 | 1.024 |
| `scale` (OS) | 1.025 | 1.008 | 1.023 |
| `scale_rep64` (OS) | 1.028 | 1.011 | 1.034 |
| `scale_rep128` (OS) | 1.020 | 1.005 | 1.024 |

손잡이를 달아줬는데 모델이 **전혀 돌리지 않았다.** 6개 실험 전부, 두 타깃 모두에서
1.0 근처(1.00~1.03)에 머물렀다. 영상 손잡이를 줄이기는커녕 오히려 아주 살짝 키웠다.
왜일까?

**이유: 손잡이가 이미 있던 것과 중복이기 때문이다.**

최종 위험점수를 만드는 계산은 이렇게 생겼다.

```
위험점수 = (영상 손잡이 × 영상 가중치 × 영상 특징)
         + (임상 손잡이 × 임상 가중치 × 임상 특징)
         + (판독지 손잡이 × 판독지 가중치 × 판독지 특징)
```

여기서 **"손잡이 × 가중치"는 결국 곱셈 하나**다. 손잡이를 2배로 키우는 것과 가중치를
2배로 키우는 것이 **완전히 똑같은 결과**를 낸다. 즉 모델 입장에서는 **원래부터 가중치로
음량을 조절할 수 있었고**, 손잡이는 새로 할 수 있는 일을 하나도 늘려주지 않았다.

> **비유**
> 스피커 음량을 조절하는 손잡이가 이미 있는데, 그 옆에 **똑같은 기능의 손잡이를 하나 더**
> 달아준 셈이다. 소리 크기를 바꿀 수 있는 범위는 전혀 늘지 않는다.

**실측으로도 확인된다.** 손잡이를 달기 전과 후의 가중치 분포가 거의 똑같다.

| PFS | 영상 | 임상 | 판독지 |
|---|---|---|---|
| `base` (손잡이 없음) | 0.473 (46%) | 0.388 (37%) | 0.178 (17%) |
| `scale` (손잡이 있음) | 0.461 (45%) | 0.386 (38%) | 0.174 (17%) |

### 4.2 그리고 `nonorm`(크기맞추기 제거)은 오히려 나빠졌다

PFS 0.6390 → **0.6306**. 크기를 안 맞추면 학습이 불안정해져서 손해만 봤다.

### 4.3 4번 제안(판독지 자리 넓히기)도 효과 없음

영상 **없이** 판독지 자리만 넓힌 대조군을 보면 분명하다.

| PFS | 판독지 16칸 | 64칸 | 128칸 |
|---|---|---|---|
| Clinical+Report | **0.6678** | 0.6286 | 0.6527 |

**자리를 넓히면 오히려 떨어진다.** 기존 16칸이 이미 잘 맞춰진 크기였고, 넓히면 외울 공간만
늘어나 과적합으로 이어진 것으로 보인다(환자가 171명뿐이라 더 그렇다).

---

## 5. 그래서 무엇을 배웠나 — 이전 진단의 수정 ★중요

이번 실험 덕분에 [RESULTS.md](RESULTS.md) **9.4절의 설명 하나가 틀렸다는 것**이 밝혀졌다.

| | 이전 진단 (9.4절) | **수정된 진단 (이번 실험)** |
|---|---|---|
| 왜 영상이 70~80%를 차지하나? | 모든 브랜치 크기를 1로 강제해서, 모델이 **영상을 줄일 수단이 구조에 없기 때문** | ✕ **틀림.** 모델은 가중치로 **얼마든지 줄일 수 있었다.** 그런데도 **스스로 영상에 큰 가중치를 주기로 선택**한 것 |
| 그럼 진짜 원인은? | (구조 문제) | **학습 과정의 문제.** 영상 특징은 CNN이 만든 128칸짜리 고분산 값이라 **학습 초반에 손실을 빨리 줄여준다.** 경사하강법이 이 "쉬운 길"을 먼저 잡아버리고, 느리게 배우는 임상·판독지가 밀려난다 |

이 현상은 멀티모달 학습 분야에서 이미 이름이 붙어 있다 —
**modality competition(모달리티 경쟁) / greedy learning(탐욕적 학습)**.

**그래서 구조를 조금 손보는 것으로는 안 고쳐진다.** 학습 과정 자체에 개입해야 한다.
이번 실험이 실패한 것이 오히려 이 결론을 강하게 뒷받침한다.

---

## 6. 다음에 무엇을 할까 — 코드 방향

이번 실험 코드(`exp_fusion_fix.py`)는 결과 확인용이므로 **삭제해도 된다.** 이 문서에 결과가
남아 있고, 아래 방향은 새로 짜는 편이 낫다.

### 우선순위 1 — GMU 스타일 게이트 (입력에 따라 변하는 문지기)

이번에 실패한 "음량 손잡이"와 결정적으로 다른 점: 손잡이는 **항상 같은 값**이라 가중치와
중복이었지만, 게이트는 **환자마다 입력을 보고 값이 달라진다.** 그래서 중복이 아니고,
실제로 표현력이 늘어난다.

```python
# 개념 (실제 구현 시 model.py에 새 클래스로)
gate = sigmoid(Linear(concat(영상특징, 임상특징, 판독지특징)))  # 환자마다 0~1 값
fused = concat(gate_img * 영상특징, gate_cli * 임상특징, gate_rep * 판독지특징)
```

- 파라미터 수백 개 수준으로 저렴 → 환자 238명에도 부담 없음
- "이 환자는 영상이 흐릿하니 덜 듣자" 같은 판단이 가능해짐
- 참고: Gated Multimodal Unit (ICLR-W 2017), Pathomic Fusion (IEEE TMI)

### 우선순위 2 — 학습 전략 교체 (구조는 그대로)

5장에서 원인이 "학습 과정"으로 특정됐으므로 이쪽이 원인에 더 직접적이다.

| 방법 | 하는 일 |
|---|---|
| **Gradient Blending** (CVPR 2020) | 모달리티별로 과적합 속도를 재서 **손실 가중치를 자동 배분** |
| **OGM-GE** (CVPR 2022) | 학습 도중 모달리티 기여도를 감시해 **기울기를 조절**. 기존 코드에 얹는 plug-in 형태 |

**구조를 안 바꾸고 학습 루프만 손보면 되므로**, `train.py`의 `train_one_epoch`에 개입하는
형태로 구현하면 된다.

### 우선순위 3 — 그냥 late fusion을 쓰기

[RESULTS.md](RESULTS.md) 8장에서 **late fusion은 이미 OS 0.722**를 냈다. concat을 고치려고
애쓰는 것보다, 이미 잘 되는 방식을 쓰는 것이 실용적일 수 있다. late fusion에서는 CoxPH가
영상 가중치를 **0.30 대 4.44**(tabular)로 알아서 낮게 잡았는데, 이것이 바로 우리가 concat에서
얻으려던 "발언권 조절"이 자동으로 일어난 예다.

---

## 6.5 ⚠️ 단, PFS는 이 모든 방향으로도 해결되지 않는다 (중요)

위 1~3번은 전부 **"섞는 방법을 개선하자"** 는 접근이다. 그런데 후속 분석
([RESULTS.md](RESULTS.md) **섹션 10**)에서 밝혀진 바로는, **PFS의 문제는 섞는 방법이 아니라
재료 자체에 있다.**

| | 문제의 정체 | 섞는 방법을 고치면? |
|---|---|---|
| **concat fusion** (이 문서의 주제) | 이미지가 발언권 70~80%를 독점 → **섞는 방법의 문제** | ✅ 해결 가능 (late fusion에서 OS 0.708 → 0.722) |
| **PFS** (섹션 10) | 이미지에 **PFS 고유 정보가 아예 없음** → **재료의 문제** | ❌ **해결 불가** |

**근거 (섹션 10 요약):** late fusion에서 CoxPH가 이미지에 준 가중치 β를 검정한 결과 —

| | OS | PFS |
|---|---|---|
| β (전체) | **+0.322, p=0.008** (0과 다름) | **+0.008, p=0.948** (0과 같음) |
| 우도비 검정 | χ²=6.69, **p=0.010** | χ²=0.00, **p=0.948** |
| 이미지를 **난수로** 대체 시 | 난수가 진짜를 이긴 비율 **0/200** | 난수가 진짜를 이긴 비율 **146/200 (73%)** |

즉 PFS에서는 **이미지를 난수로 바꿔도 결과가 같다.** 발언권을 아무리 정교하게 조절해도,
애초에 전달할 정보가 없으면 성능은 오르지 않는다.

**따라서 위 1~2번(GMU 게이트 / Gradient Blending)을 시도할 때는:**

- **기대 효과는 OS에 한정**해서 잡아야 한다. concat 방식으로도 late fusion(0.722)에 근접하거나
  넘어서는 것이 현실적 목표다.
- **PFS에서 개선이 나온다면 의심해봐야 한다** — 5-fold 편차(±0.05)가 크므로 우연일 가능성이
  높다. 반드시 난수 대조군과 비교할 것.
- **PFS에서 영상을 살리고 싶다면 fusion이 아니라 영상 표현 자체를 바꿔야 한다:**
  치료 전/후 영상의 **변화량**(치료 반응 대리 지표), 종양 분할 후 **radiomics 특징**,
  또는 다병변 정보 활용 등.

### 하지 말아야 할 것 (이번 실험으로 확인됨)

- ✕ 브랜치마다 **고정된** 스케일 파라미터 추가 → 수학적으로 무의미
- ✕ `F.normalize` 제거 → 학습만 불안정해짐
- ✕ 판독지 브랜치 차원 확대 → 과적합만 늘어남

---

## 7. 재현 / 산출물

```bash
python exp_fusion_fix.py --target pfs      # 10개 변형 전부 (~90분)
python exp_fusion_fix.py --target os
```

```
outputs/fusion_fix/results_{os,pfs}.json   # 변형별 fold C-index + 학습된 손잡이 값
```

**한계 명시**

- 5-fold 기준이라 **fold별 편차(±0.05)가 변형 간 차이(±0.02)보다 크다.** 따라서 이 실험은
  "어떤 변형이 조금 더 좋다"를 가릴 수는 없고, **"목표선을 넘은 변형이 없다"** 는 것만
  말할 수 있다. 그 결론은 명확하다.
- 학습된 손잡이 값이 1.0에 머문 것(4.1)은 **fold와 무관하게 모든 실험에서 관측**되었으므로,
  성능 수치보다 훨씬 신뢰도 높은 근거다.
